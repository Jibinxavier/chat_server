get simple communication between the client and server DONE
get a client to join a chat room (create a dictionary with ref as key and clients as list values)
test out the joining methods


/// notes 
need to fix the synchronisation between incomming and outgoing channels

// at the moment client -> chatromm -> client